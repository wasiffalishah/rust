fn main(){
    let mut x = 4;
    x = 6;
    
    println!("{}",x);
    
    //the vairable are immutable in rust, you cannot change it directly, if you want to chnage them then youu have to use keyword `mut` before the variabel name, in our chase let mut x = 4
}

